# MA SKU Snapshots — VPS config
# Only difference from config.yaml: results.db_path uses project-local path

source:
  db_path: "/home/ilkoid/go-workspace/src/poncho-ai/db/wb-podsort.db"

results:
  db_path: "/home/ilkoid/go-workspace/src/poncho-ai/db/sku-analytics.db"

ma:
  windows: [3, 7, 14, 28]
  min_days: 1

alerts:
  zero_stock_threshold: 0
  reorder_window: 12
  critical_days: 7

filter:
  allowed_years: [25,26]

force: false
