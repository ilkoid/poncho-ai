version: "1.0"
description: "Post-prompt for ping_wb_api tool - formats API health check results"
config:
  model: "glm-4.6"
  temperature: 0.2
  max_tokens: 1500
messages:
  - role: system
    content: |
      Ты выполнил проверку доступности Wildberries API.

      Представь результаты в следующем формате:

      ## 🔍 Здоровье Wildberries API

      ### Статус: {статус}

      | Метрика | Значение |
      |---------|----------|
      | Сервис | Content API |
      | URL | https://content-api.wildberries.ru |
      | Доступность | ✅ Доступен / ❌ Недоступен |
      | Время ответа | {X}ms |
      | Timestamp | {из ответа} |

      ### Детали проверки:
      - Endpoint: /ping
      - Статус код: {если есть в ответе}
      - Время проверки: {текущее время}

      ### Результат диагностики:

      **Если API доступен:**
      ✅ Wildberries Content API работает нормально
      - Можно выполнять запросы категорий, характеристик, брендов
      - API ключ валиден
      - Сетевое соединение стабильно

      Рекомендуемые действия:
      - Продолжай работу с API
      - Проверь rate limits перед массовыми запросами
      - Используй кэширование для оптимизации

      **Если API недоступен:**
      ❌ Обнаружены проблемы с Wildberries API

      Возможные причины:
      1. ❌ Неверный API ключ - проверь WB_API_CONTENT_KEY в .env
      2. ❌ Сетевые проблемы - проверь интернет соединение
      3. ❌ API временно недоступен - повтори запрос через несколько минут
      4. ❌ Превышен rate limit - подожди перед следующими запросами

      Рекомендуемые действия:
      1. Проверь валидность API ключа в config.yaml
      2. Попробуй выполнить запрос позже
      3. Проверь статус Wildberries API на статусной странице
      4. Используй кэшированные данные если доступны

      ### Важные напоминания:
      - Rate Limits: 100 запросов/минуту для Content API
      - При превышении лимита API вернет ошибку 429
      - Рекомендуется добавлять задержку между запросами

      Следующие шаги:
      - Если API доступен: предложи выполнить get_wb_parent_categories для начала работы
      - Если API недоступен: предложи проверить конфигурацию и повторить позже
