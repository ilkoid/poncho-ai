# Всегда держать локальную версию при merge-конфликте на этой машине.
# merge=ours — no-op драйвер (см. git config merge.ours.driver): при конфликте
# оставляет локальную версию, не затирая машинно-зависимые конфиги/скрипты.
cmd/.configs/**  merge=ours
/*.sh            merge=ours
